const User = require('../models/user_model');

exports.addUser = async(req, res, next) => {
    try {
        const { studentID, fname, lname, image, role, password } = req.body;

    } catch (error) {

    }

}